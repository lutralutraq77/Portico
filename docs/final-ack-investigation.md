# Final acknowledgment and forwarding completion

The subsequent [client completion and daemon readiness correction](client-completion.md) adds a client-side application handoff barrier after reproducing graceful connector closure during the final read. It supersedes the current-source qualification wording below; all earlier results remain historical evidence.

The shutdown changes have passed native regression and static checks. Full current-source qualification is pending. The latest local Arch run passed 18 of 21 cases, then failed three positive controls before any destination connection. The [current evidence](phase-6-final-ack-evidence.json) records the exact source, successful checks and failed guest run. Phases 6 and 7 remain incomplete.

Two problems were identified. First, the connector could close its asynchronous carrier while its acknowledgment of the client's FIN was still queued. The client now acknowledges the connector FIN only after receiving acknowledgment of its own FIN. `Conn.WaitFinished` joins connector-initiated closure and requires both validated FIN directions and the client's outgoing acknowledgment.

Second, the connector previously sent its acknowledgment as soon as the framing reader consumed input. A later forwarding or destination half-close failure could then race a successful client completion. The connector now permits its acknowledgment only after both forwarding workers succeed. A forwarding failure closes the stream without granting that completion proof. Operation deadlines, revocation, lease checks and final agent RPC status remain enforced.

The diagnostic identified destination `CloseWrite` failures in the positive HTTPS fixture: it immediately closed application TLS/TCP after sending its response. The positive destination now exchanges TLS close-notify and observes the client's TCP FIN before closing, with no trailing application or transport data permitted. It retains the existing deadline, exact binary response, authentication, Host/SNI, socket-count, closure-receipt, endpoint-cleanup and network-invariance checks. Twelve additional fresh positive controls exercise the ordering repeatedly. Abrupt forwarding errors are not converted into success.

The final native workload/client/agent race run passed 33 top-level tests and 121 subcases, with no failures or skips. Native and Linux vet/staticcheck and the source secret scan passed. Independent compiler-overlay controls prove both regressions: removing client ACK ordering loses the queued acknowledgment, and removing the forwarding barrier acknowledges input before forwarding succeeds. These are expected failing controls, separate from the successful unmodified tests.

The current uninstrumented Arch run failed overall in 173.83 seconds. Eleven authenticated positive controls completed, including the original control after denials and repeats 00 through 08. Repeats 09 through 11 failed with zero destination/HTTP/authentication counts; the connector returned a health rejection at cleanup. Inspection points to its terminal clock-health path, but this run does not establish which clock sample check failed. The guest's earlier transfer failures did not recur in the completed controls. This observation is not a full application pass; hosted Linux qualification remains required.

All earlier failures remain preserved. The first candidate passed 16 of 21 cases; its [investigation evidence](final-ack-investigation-evidence.json) retains source hashes, unsuccessful harness attempts and the corrected negative control. The subsequent closure diagnostic passed 19 cases and failed two, exposing the premature destination acknowledgment. Historical hosted success on `1c1bd60` applies only to that revision. No additional canonical acceptance scenario is claimed: seven are implemented and 84 remain planned.

# Final-response drain ordering

The client bounds its final local output write when it receives a validated peer FIN. Graceful carrier closure waits for successful local forwarding, so closure cannot be the event that starts that forwarding deadline. The FIN signal grants no new read/write authority and does not close the independent input direction. A later carrier event cannot extend an already established drain deadline.

The current correction follows hosted source `9f441b0` and combined dashboard source `dcf8002`. Each passed five workflows but failed ordinary Linux qualification in two configured-client subcases. Both tests waited for a closure receipt while stdout remained unread, contradicting the new client completion gate. Review also exposed that the production drain deadline was still started only by carrier closure.

The configured-client tests now require the destination to have closed, require completion to remain unreported while the final output is unread, and then exercise both outcomes. The positive case drains a response larger than the measured pipe capacity and requires exact bytes, successful process exit and the eventual closure receipt. The negative case preserves the six-second observation window for the five-second drain limit and requires failed process exit and a closure receipt. There is no timeout increase or conversion of forwarding failure into success.

Native workload/client race tests passed in 30.183 and 52.362 seconds. Windows/Linux vet and staticcheck passed. The targeted real Linux guest passed eight workload tests and thirteen subcases overall, but failed daemon startup and exited the connector on a security check before either new transfer case could open an application. It failed overall with two top-level failures and five subcase failures; that run does not qualify the changed drain behavior. Its exact source, binaries, image and log are retained in the [evidence record](phase-6-fin-drain-evidence.json).

Hosted sources `c703ddc` and dashboard `b1be43a` both passed all eleven configured-client subcases, including both changed final-response tests. Their full Linux runs nevertheless failed the same separate case: a fresh transfer after manual daemon lock and explicit re-unlock, before a second destination connection or session authorization. Main passed 228 top-level tests and 1,107 subcases; dashboard passed 246 and 1,148. Each had one top-level/subcase failure and four expected skips. The tested merge trees equal their respective source trees, and downloaded Linux artifact digests were verified in the [hosted evidence record](phase-6-fin-drain-hosted-evidence.json).

Main passed four hosted workflows and failed Linux and Arch service qualification. The service run passed all four installed-service cases and 20 of 21 HTTPS cases; positive repeat 09 failed after authentication, while the next two positive controls passed. Dashboard passed five workflows and failed Linux. Its verified service artifact passed all four installed-service cases in 51.74 seconds and all 21 HTTPS cases in 42.72 seconds. Neither failure has an established root cause. A local instrumented Arch guest first failed virtual timer initialization; one identical reboot reached the test but its curl version command exceeded the fixture deadline before any transfers. These diagnostics supply no application qualification. A smaller daemon-specific diagnostic passed all 17 subcases in 233.10 seconds with compiler-only failure-stage observation; it did not reproduce the hosted rejection. That diagnostic cannot replace the failed uninstrumented runs.

The subsequent [fresh-binding fixture correction](daemon-binding-fixture.md) passed its uninstrumented local Linux run and all six PR workflows on both subsequent sources 30974c8 and 5afced8. Both full Linux and Arch service/application guests passed; [source-bound evidence](phase-6-fresh-binding-hosted-evidence.json) records the counts, merge trees and verified artifacts. Earlier local and hosted failures remain recorded. Phases 6 and 7 are incomplete; the canonical acceptance ledger remains seven implemented and 84 planned out of 91.
